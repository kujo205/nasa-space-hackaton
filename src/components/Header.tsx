import { Button } from "@/components/ui/button";

export default function Header() {
  return (
    <header className="w-full max-w-4xl mx-auto bg-background/50 border px-10 py-4 sticky top-2 rounded-lg backdrop-blur-sm z-10">
      <Button> TEST</Button>
    </header>
  );
}
